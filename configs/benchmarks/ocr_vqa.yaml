# OCR Benchmarking configuration
task: "ocr"
benchmark:
  eval_dataset: "naver-clova-ix/cord-v2"
  eval_samples: 50
  sample_prompts:
    - "Extract all items and prices from this receipt."
  max_new_tokens: 512
  warmup_runs: 2
  timed_runs: 5
