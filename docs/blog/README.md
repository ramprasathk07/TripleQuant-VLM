# Blog posts

Written from this repo's measured results. Every figure is generated by
`scripts/make_blog_plots.py`, which reads the committed JSON under `results/` — no number
is typed in by hand, so re-running the benchmarks regenerates correct figures.

| # | Post | Core finding |
|---|---|---|
| 1 | [Your quantized model isn't slow. Your runtime is.](01-your-quantized-model-isnt-slow.md) | The same AWQ-W4A16 checkpoint: 4.3 tok/s on HF eager, 57.9 on vLLM — 13.6x, weights unchanged. Fused kernels, not the format, decide speed. |
| 2 | [Three times I measured nothing and didn't notice](02-the-metric-couldnt-see-it.md) | Three claims that were structurally unsupported: PPL can't see KV-cache error, bare accuracy hides its sample size, a memory probe says nothing about quality. |
| 3 | [KV-cache compression: 4x the context, and what it actually costs](03-kv-cache-compression-what-it-costs.md) | 4x context is real — at K8V8 (92% agreement), not the K3V2 the README showcased (19%). The quality cliff is the bit budget, not context length. |

Regenerate the figures:

```bash
python scripts/make_blog_plots.py
```

Palette is the validated categorical set from the `dataviz` skill (light + dark steps
checked for lightness band, chroma floor, CVD separation, normal-vision floor, and
contrast). Marks carry direct labels because two light-mode hues fall below 3:1 against
the surface — identity is never conveyed by color alone.
